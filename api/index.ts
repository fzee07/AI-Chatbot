// Vercel Serverless Entry Point
// Vercel auto-detects files in /api and turns them into serverless functions.
// This file imports the Express app and exports it as the default handler.

import app from "../src/app";

export default app;
